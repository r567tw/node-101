version:  '3.7'
services:
  mysql:
    image: mysql:latest
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: node
      MYSQL_USER: node
      MYSQL_PASSWORD: node
    ports:
      - 3306:3306